<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/salary_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/accent"
    android:alpha="0.8"
    android:paddingBottom="@dimen/salary_list_item_text_padding_vertical"
    android:paddingTop="@dimen/salary_list_item_text_padding_vertical">

    <TextView
        android:id="@+id/date_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingEnd="@dimen/salary_list_item_text_padding_side"
        android:paddingStart="@dimen/salary_list_item_text_padding_side"
        android:textSize="@dimen/salary_list_item_text_size"
        android:textColor="@color/primary_dark"
        tools:text="July 2018" />

</FrameLayout>