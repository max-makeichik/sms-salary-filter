<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_salaries_graph"
        android:icon="@drawable/ic_chart_line"
        android:title="@string/graph_title"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_salaries_list"
        android:icon="@drawable/ic_list"
        android:title="@string/list_title"
        android:visible="false"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_prefs"
        android:icon="@drawable/ic_prefs"
        android:title="@string/prefs"
        app:showAsAction="never" />

</menu>